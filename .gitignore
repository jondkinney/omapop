__pycache__/
*.pyc

# The scraped catalogue is a local per-user cache, not a mirror we republish.
directory.json

# Reproducible staging; signed port archives and their source are versioned.
ports/build/
ports/candidates.json
